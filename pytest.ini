[pytest]
minversion = 7.0
testpaths =
    app
    tests
asyncio_mode = auto
addopts =
    --cov=app
    --cov-report=term-missing
    --cov-report=html:coverage/coverage_html
    --cov-report=lcov:coverage/coverage.lcov
